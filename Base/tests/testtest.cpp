#include "test/testframework.hpp"

TEST(TestSystemTests, ExpectTrueFalse)
{
  CheckMemory memory;
  {
	  /*
    auto throwLambda = [](){throw std::string();};
    EXPECT_TRUE(true);
    EXPECT_FALSE(false);
    EXPECT_THROW(throwLambda(), std::string);
	*/
  }
}
